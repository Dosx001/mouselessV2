{
  "name": "mouselessV2",
  "version": "1.0.0",
  "description": "Quick keyboard access to links and other interactive elements",
  "scripts": {
    "build": "web-ext build -s dist -i '**/*.css.map' '**/*.kra' '**/*.sh' --overwrite-dest",
    "tar": "tar czvf mouselessV2.tar.gz src dist build.sh package.json tsconfig.json yarn.lock README_FIREFOX.md",
    "sass": "sass --update src:dist"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^120.0.4",
    "@typescript-eslint/eslint-plugin": "^7.15.0",
    "@typescript-eslint/parser": "^7.15.0",
    "eslint": "^9.6.0",
    "globals": "^15.8.0",
    "typescript": "^5.5.3"
  }
}
